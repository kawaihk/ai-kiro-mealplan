-- ============================================================
-- IT試験用 PostgreSQL 初期化スクリプト
-- コンテナ初回起動時（ボリュームが空の場合）のみ実行される
--
-- Spring Boot の ddl-auto=create-drop がテーブルを管理するため、
-- ここでは拡張機能の有効化のみ行う
-- ============================================================

-- UUID 拡張（将来的な利用に備えて有効化）
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
