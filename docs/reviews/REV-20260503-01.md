# レビュー記録票: src/main 配下ソースコード総合レビュー

## 1. レビュー情報

- **レビューID**: REV-20260503-01
- **レビュー対象**: `src/main` 配下の全ソースコード（Java / HTML / CSS / JavaScript）
- **レビュアー**: Kiro
- **レビュー日**: 2026-05-03
- **ステータス**: 指摘あり

---

## 2. チェック結果サマリー

全体的にSpring Boot 3.x の標準的な構成に沿っており、例外ハンドリングやバリデーションの仕組みは適切に設計されています。一方で、複数のクラスが空実装（スケルトン）のままであること、`UserGoalRepository` が存在しないエンティティを参照していること、フロントエンドの `app.js` に重大な構造的バグが含まれていることなど、機能的に問題となる指摘が複数確認されました。

---

## 3. 指摘事項

| ID | 分類 | 対象ファイル | 内容 | 影響/リスク | 対応方針 | ステータス | 対応結果 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| R001 | **バグ** | `static/js/app.js` | `loadRecipes()` 関数が同一スコープ内に2回定義されている。2つ目の定義は `try-catch` を持たず、1つ目の定義を上書きする。 | エラー時のハンドリングが失われ、通信失敗時に例外が握りつぶされる。 | 重複している2つ目の `loadRecipes()` を削除する。 | **完了** | `try-catch` を持たない2つ目の `loadRecipes()` 定義を削除。`try-catch` 付きの1つ目の定義のみを残した。 |
| R002 | **バグ** | `static/js/app.js` | `resetForm()` 関数の内部に `window.editRecipe`、`window.deleteRecipe`、`escapeHtml` の定義が混在しており、関数の閉じ括弧 `}` の位置が誤っている。`escapeHtml` が `resetForm` のスコープ内に閉じ込められており、外部から呼び出せない。 | `renderTable` 内で `escapeHtml` を呼び出しているが、`resetForm` 実行前は参照できず実行時エラーになる。 | `window.editRecipe`、`window.deleteRecipe`、`escapeHtml` を `resetForm` の外に移動し、スコープを整理する。 | 完了 | `window.editRecipe`、`window.deleteRecipe`、`escapeHtml` を `resetForm` の外（`DOMContentLoaded` コールバック直下）に移動。`resetForm` はフォームのリセット処理のみを担うよう整理した。 |
| R003 | **バグ** | `static/js/app.js` | `window.deleteRecipe` の閉じ括弧 `}` の後に `};` が続いており、`resetForm` 関数の閉じ括弧が欠落している。構文エラーによりスクリプト全体が動作しない可能性がある。 | ページ全体のJavaScriptが実行されない。 | ブラケットの対応を修正し、各関数のスコープを正しく閉じる。 | 完了 | `app.js` のブラケット対応を修正。`window.editRecipe`・`window.deleteRecipe`・`escapeHtml` を `resetForm` の外に移動し、各関数のスコープを正しく整理した。 |
| R004 | **コンパイルエラー** | `repository/UserGoalRepository.java` | `JpaRepository<UserGoal, Long>` を参照しているが、`UserGoal` エンティティクラスが存在しない（`UserGoalDto` に変更済み）。 | ビルドエラーが発生し、アプリケーションが起動しない。 | `UserGoal` エンティティを新規作成するか、リポジトリの型パラメータを正しいエンティティに修正する。 | **完了** | `entity/UserGoal.java` を新規作成し、`UserGoalRepository` のインポートを `com.aimealplan.model.UserGoal` から `com.aimealplan.entity.UserGoal` に修正。コンパイルエラーを解消した。 |
| R005 | **未実装** | `controller/MealController.java` | クラス本体が空（`public class MealController {}`）。 | Meal に関するAPIエンドポイントが一切存在しない。 | 要件に基づき、Meal の CRUD エンドポイントを実装する。 | **完了** | `MealService` インターフェース（CRUD メソッド定義）、`MealServiceImpl`（Entity↔DTO変換・例外処理含む）、`MealController`（`/api/meal-plans/{mealPlanId}/meals` 配下の CRUD エンドポイント）を実装。 |
| R006 | **未実装** | `controller/UserController.java` | クラス本体が空（`public class UserController {}`）。 | ユーザー登録・取得などのAPIエンドポイントが存在しない。 | 要件に基づき、User の CRUD エンドポイントを実装する。 | **完了** | R009 対応時に `UserController` に POST/GET（一覧・単件）/PUT/DELETE エンドポイントを実装。パスワードは `Map<String, String>` で受け取り、レスポンスの `UserDto` には含めない設計。 |
| R007 | **未実装** | `controller/UserGoalController.java` | クラス本体が空（`public class UserGoalController {}`）。 | ユーザー目標設定に関するAPIエンドポイントが存在しない。 | 要件に基づき、UserGoal の CRUD エンドポイントを実装する。 | 完了 | `UserGoalController` に POST/GET/PUT/DELETE エンドポイントを実装。合わせて `UserGoalService` インターフェース（R010）、`UserGoalServiceImpl`（R013）、`UserGoal` エンティティ（R004対応）も実装した。 |
| R008 | **未実装** | `service/MealService.java` | インターフェース本体が空（`public interface MealService {}`）。 | サービス層のコントラクトが未定義。 | Meal の業務ロジックに必要なメソッドを定義する。 | **完了** | `createMeal`・`getMealsByMealPlanId`・`getMealById`・`updateMeal`・`deleteMeal` の5メソッドを定義済み。入出力は `MealDto` を使用。 |
| R009 | **未実装** | `service/UserService.java` | インターフェース本体が空（`public interface UserService {}`）。 | サービス層のコントラクトが未定義。 | User の業務ロジックに必要なメソッドを定義する。 | **完了** | `UserService` に `createUser` / `getAllUsers` / `getUserById` / `updateUser` / `deleteUser` の5メソッドを定義。`UserServiceImpl` で Entity↔DTO変換・例外処理・`@Transactional` 制御を実装。`UserController` で `/api/users` 配下の CRUD エンドポイントを実装（パスワードはレスポンスに含めない設計）。 |
| R010 | **未実装** | `service/UserGoalService.java` | インターフェース本体が空（`public interface UserGoalService {}`）。 | サービス層のコントラクトが未定義。 | UserGoal の業務ロジックに必要なメソッドを定義する。 | 完了 | `createUserGoal` / `getUserGoalByUserId` / `updateUserGoal` / `deleteUserGoal` の4メソッドを定義。R007（UserGoalController）対応時に合わせて実装済み。 |
| R011 | **未実装** | `service/impl/MealServiceImpl.java` | クラス本体が空（`public class MealServiceImpl {}`）。`MealService` を実装していない。 | Meal に関するビジネスロジックが存在しない。 | `MealService` インターフェースを実装し、業務ロジックを記述する。 | 完了 | `MealService` を実装。`MealPlanRepository` で親の存在確認を行い、Entity↔DTO変換・`ResourceNotFoundException` による例外処理・`@Transactional` 制御を含む CRUD ロジックを実装済み。 |
| R012 | **未実装** | `service/impl/UserServiceImpl.java` | クラス本体が空（`public class UserServiceImpl {}`）。`UserService` を実装していない。 | User に関するビジネスロジックが存在しない。 | `UserService` インターフェースを実装し、業務ロジックを記述する。 | **完了** | `UserService` を実装済み。`createUser`・`getAllUsers`・`getUserById`・`updateUser`・`deleteUser` の5メソッドを実装。`UserRepository` を DI し、エンティティ↔DTO 変換（`toDto`）も含む。 |
| R013 | **未実装** | `service/impl/UserGoalServiceImpl.java` | クラス本体が空（`public class UserGoalServiceImpl {}`）。`UserGoalService` を実装していない。 | UserGoal に関するビジネスロジックが存在しない。 | `UserGoalService` インターフェースを実装し、業務ロジックを記述する。 | 完了 | R007対応時に `UserGoalService` を実装。`UserGoalRepository` を使った CRUD ロジック・Entity↔DTO変換を実装済み。 |
| R014 | **設計** | `controller/RecipeController.java` | `@RequestParam(required = true)` は `required` のデフォルト値が `true` であるため冗長。また、`keyword` が空文字の場合の考慮がない。 | 空文字を渡すと全件検索と同等の結果が返る可能性がある。 | `required = true` を削除し、空文字チェックを追加する（またはサービス層で対応）。 | 完了 | `required = true` を削除。コントローラ層で `keyword` が `null` または空文字・空白のみの場合は `getAllRecipes()` にフォールバックするよう空文字チェックを追加した。 |
| R015 | **設計** | `service/RecipeService.java` | サービスインターフェースが `Recipe` エンティティを直接入出力として使用している。 | コントローラ層とサービス層がエンティティに直接依存し、APIレスポンスの形式変更が困難になる。 | `RecipeDto` を導入し、サービスインターフェースの入出力を DTO に変更することを検討する。 | **完了** | `model/RecipeDto.java` を新規作成。`RecipeService` の全メソッドの入出力を `RecipeDto` に変更。`RecipeServiceImpl` に `toDto`・`toEntity` マッピングメソッドを追加。`RecipeController` も `RecipeDto` を使用するよう修正し、エンティティへの直接依存を排除した。 |
| R016 | **設計** | `entity/MealPlan.java` | `createdAt` フィールドに `@CreationTimestamp` が付与されておらず、手動での設定が必要。`Recipe.java` では `@CreationTimestamp` / `@UpdateTimestamp` が使用されており、一貫性がない。 | 作成日時が設定されないまま保存される可能性がある。 | `@CreationTimestamp` アノテーションを追加し、自動設定されるようにする。 | 完了 | `MealPlan.java` の `createdAt` に `@CreationTimestamp`、`updatedAt` に `@UpdateTimestamp` が既に付与済み。`Recipe.java` と同様の構成で一貫性が保たれている。 |
| R017 | **設計** | `entity/User.java` | `role` フィールドが `String` 型で定義されており、任意の文字列が設定可能。 | 不正なロール値が設定されるリスクがある。 | `enum Role { USER, ADMIN }` を定義し、`@Enumerated(EnumType.STRING)` で管理することを検討する。 | 完了 | `entity/Role.java` に `enum Role { USER, ADMIN }` を定義済み。`User.java` の `role` フィールドを `Role` 型に変更し、`@Enumerated(EnumType.STRING)` を付与済み。 |
| R018 | **設計** | `model/MealDto.java` | `calories` と `protein` フィールドが `Integer` 型だが、`@PositiveOrZero` などのバリデーションアノテーションが付与されていない。 | 負の値が入力された場合にバリデーションエラーにならない。 | `@PositiveOrZero` アノテーションを追加する。 | 完了 | `calories`・`protein` フィールドに `@PositiveOrZero` を追加。`jakarta.validation.constraints.PositiveOrZero` をインポートした。 |
| R019 | **設計** | `validation/PfcRatioSumValidator.java` | PFC比率のいずれかが `null` の場合、`0` として扱い合計が100にならなくても `false` を返す。しかし、全て `null` の場合（未設定）は合計が `0` となり `false` を返すため、目標未設定のユーザーが登録できない。 | 目標カロリーのみ設定してPFC比率を未設定にするユースケースが実現できない。 | 全フィールドが `null` の場合は `true` を返す（未設定を許容する）か、要件に応じてバリデーションロジックを見直す。 | 完了 | 全フィールドが `null` の場合は `true` を返すよう修正。一部でも設定されている場合は合計が100であることを検証する。 |
| R020 | **セキュリティ** | `static/js/app.js` | 検索キーワードを `fetch` のURLに直接埋め込んでいる（`encodeURIComponent` は使用済み）が、APIレスポンスの `recipe.calories` を `escapeHtml` せずに直接 `innerHTML` に埋め込んでいる。 | `calories` に悪意あるスクリプトが含まれていた場合、XSSが発生する可能性がある（サーバー側でも対策が必要）。 | `recipe.calories` も `escapeHtml` を通すか、`textContent` を使用して安全に描画する。 | **完了** | `renderTable` 内の `recipe.calories` を `escapeHtml(String(recipe.calories))` でエスケープするよう修正。`innerHTML` への直接埋め込みによる XSS リスクを解消した。 |
| R021 | **セキュリティ** | `entity/User.java` | `password` フィールドに `@Column(nullable = false)` のみが設定されており、パスワードのハッシュ化に関する実装が確認できない。 | 平文パスワードがDBに保存されるリスクがある。 | `UserServiceImpl` でのユーザー登録時に `BCryptPasswordEncoder` 等でハッシュ化する実装を必ず追加する。 | **完了** | `config/SecurityConfig.java` を新規作成し `BCryptPasswordEncoder` を `PasswordEncoder` Bean として登録。`UserServiceImpl` に `PasswordEncoder` を DI し、`createUser` で `passwordEncoder.encode(rawPassword)` によるハッシュ化を実装。合わせて `UserController` の `role(String)` 型不一致エラーも修正（`Role.valueOf()` で変換）。 |
| R022 | **品質** | `static/js/constants.js` / `static/js/utils/apiClient.js` | 両ファイルの内容が `// logic` のみ（未実装）。 | 定数管理・API通信の共通化が機能していない。`app.js` 内で直接 `fetch` を呼び出しており、共通化の恩恵が得られていない。 | `constants.js` に `API_BASE` 等の定数を移動し、`apiClient.js` に共通の `fetch` ラッパーを実装する。 | 完了 | `constants.js` に `API_BASE` を定義。`apiClient.js` に `get` / `post` / `put` / `del` の共通 `fetch` ラッパーを実装（HTTP エラーを例外としてスロー）。`app.js` から `API_BASE` の直書きを削除し、各 `fetch` 呼び出しを `apiClient` の関数に置き換えた。ES Modules 形式で `import` / `export` を使用（HTML 側は既に `type="module"` 設定済み）。 |
| R023 | **品質** | `static/index.html` | ファイルの内容が `<!DOCTYPE html><html><head><meta charset='UTF-8'></head><body></body></html>` のみ（空ページ）。 | ルートパス (`/`) にアクセスしても何も表示されない。 | `static/html/index.html` の内容をこちらに統合するか、適切なコンテンツを配置する。 | **完了** | `static/html/index.html` の内容を `static/index.html` に統合。CSS パスを `style.css` → `css/style.css`、JS パスを `app.js` → `js/app.js` に修正し、`static/` 直下からの相対パスに合わせた。 |
| R024 | **品質** | `repository/MealPlanRepository.java` / `repository/MealRepository.java` | `@Repository` アノテーションが付与されていない。`RecipeRepository` や `UserGoalRepository` には付与されており、一貫性がない。 | Spring Data JPA では省略可能だが、明示的に付与することでコードの意図が明確になる。 | `@Repository` アノテーションを追加し、一貫性を保つ。 | **完了** | `MealPlanRepository` と `MealRepository` の両ファイルに `@Repository` アノテーションおよび `org.springframework.stereotype.Repository` のインポートを追加。全リポジトリクラスで統一した。 |

---

## 4. 総評および次のステップ

### 総評

`RecipeController` / `RecipeServiceImpl` / `Recipe` エンティティ周辺は比較的完成度が高く、例外ハンドリングやバリデーションの仕組みも適切に設計されています。一方で、Meal・User・UserGoal に関するコントローラ・サービス・サービス実装クラスがすべてスケルトン状態であり、アプリケーションとして動作する状態にありません。また、フロントエンドの `app.js` には構文的・論理的なバグが複数含まれており、優先的な修正が必要です。

### 優先度別 次のステップ

**高優先度（アプリ起動・動作に直結）**
1. R004: `UserGoalRepository` のコンパイルエラーを解消する
2. R001〜R003: `app.js` の構造的バグを修正する

**中優先度（機能実装）**
3. R005〜R013: 空実装のコントローラ・サービス・サービス実装を順次実装する

**低優先度（品質・設計改善）**
4. R014〜R024: 設計改善・セキュリティ・品質向上の対応を行う

---

*このレビュー記録票は指摘事項ごとにステータスを管理します。対応完了後は「ステータス」欄を「完了」に更新し、「対応結果」欄に実施内容を記載してください。*
