# レビュー記録票: src/main 配下ソースコード第3回レビュー

## 1. レビュー情報

- **レビューID**: REV-20260504-01
- **レビュー対象**: `src/main` 配下の全ソースコード（Java / HTML / CSS / JavaScript）
- **前回レビュー**: REV-20260503-02
- **レビュアー**: Kiro
- **レビュー日**: 2026-05-04
- **ステータス**: 指摘あり

---

## 2. チェック結果サマリー

前回レビュー（REV-20260503-02）の主要指摘事項は概ね対応済みであり、DTO 導入・パスワードハッシュ化・バリデーション強化・フロントエンドのモジュール化など、全体的な品質は大幅に向上しています。今回は残存する設計上の問題・セキュリティリスク・品質課題を中心に新規指摘事項を抽出しました。

---

## 3. 指摘事項

| ID | 分類 | 対象ファイル | 内容 | 影響/リスク | 対応方針 | ステータス | 対応結果 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| R201 | **セキュリティ** | `controller/UserController.java` | `updateUser` で `UserDto` をリクエストボディとして受け取っているが、`UserDto` にはバリデーションアノテーションが付与されていない。`username` や `email` が `null` や空文字のまま更新できてしまう。 | 不正な値でユーザー情報が上書きされる可能性がある。 | `UserDto` に `@NotBlank`・`@Email` 等のバリデーションを追加するか、更新専用の `UserUpdateRequest` クラスを導入して `@Valid` と組み合わせる。 | **完了** | `model/UserUpdateRequest.java` を新規作成し、`username`（`@NotBlank` + `@Size(max=50)`）・`email`（`@NotBlank` + `@Email`）・`role`（任意）フィールドを定義。`UserController.updateUser` のリクエストボディを `UserDto` から `@Valid UserUpdateRequest` に変更。コントローラ側で `UserDto` に変換してサービス層へ渡す設計とし、サービス層への影響を最小化した。 |
| R202 | **セキュリティ** | `controller/UserController.java` | `createUser` で `Role.valueOf(roleStr.toUpperCase())` を呼び出しているが、`GlobalExceptionHandler` に `IllegalArgumentException` ハンドラーは存在するものの、エラーメッセージに内部の enum 名が含まれる可能性がある。また、ロール文字列の検証をコントローラ層で行っており、サービス層との責務分離が不明確。 | 不正なロール値を渡した際のエラーメッセージが詳細すぎる可能性がある。 | `UserCreateRequest.role` に `@Pattern(regexp = "USER\|ADMIN", message = "ロールは USER または ADMIN を指定してください")` を追加し、コントローラ到達前にバリデーションで弾く。 | 完了 | `UserCreateRequest.role` に `@Pattern(regexp = "(?i)USER\|ADMIN", message = "ロールは USER または ADMIN を指定してください")` を追加。大文字・小文字どちらも許容しつつ、それ以外の値はバリデーション（`MethodArgumentNotValidException` → 400 Bad Request）で弾くようにした。コントローラの `Role.valueOf(roleStr.toUpperCase())` は `@Pattern` 通過後の値のみ到達するため `IllegalArgumentException` は発生しない。 |
| R203 | **セキュリティ** | `service/impl/UserServiceImpl.java` | `updateUser` でユーザー名・メールアドレスの重複チェックが行われていない。別ユーザーが使用中の `username` や `email` に更新できてしまう。 | DB の `UNIQUE` 制約違反で `DataIntegrityViolationException` が発生し、`GlobalExceptionHandler` に対応するハンドラーがないため 500 エラーになる。 | `updateUser` 内で `userRepository.findByUsername` / `findByEmail` を使って重複チェックを行い、重複時は適切なエラーを返す。または `GlobalExceptionHandler` に `DataIntegrityViolationException` ハンドラーを追加して 409 Conflict を返す。 | 完了 | `DuplicateResourceException`（409 Conflict）を新規作成。`updateUser` に自分以外との `username` / `email` 重複チェックを追加し、重複時にスロー。`createUser`（R204）も同様に重複チェックを追加。`GlobalExceptionHandler` に `handleDuplicateResourceException` を追加し 409 Conflict を返すよう対応。 |
| R204 | **セキュリティ** | `service/impl/UserServiceImpl.java` | `createUser` でも同様に、`username` および `email` の重複チェックが行われていない。 | DB の `UNIQUE` 制約違反で 500 エラーになる。 | `createUser` 内で `userRepository.findByUsername` / `findByEmail` を使って重複チェックを行い、重複時は 409 Conflict を返す。または R203 と共通で `DataIntegrityViolationException` ハンドラーを追加する。 | **完了** | `exception/DuplicateResourceException.java` を新規作成。`UserServiceImpl.createUser` で `findByUsername` / `findByEmail` による重複チェックを追加し、重複時は `DuplicateResourceException` をスロー。`GlobalExceptionHandler` に `handleDuplicateResourceException` ハンドラーを追加し、409 Conflict を返すようにした。 |
| R205 | **設計** | `controller/MealController.java` | `deleteMeal` で `@PathVariable Long mealPlanId` を受け取っているが、サービス層に渡しておらず使用されていない。異なるミールプランの Meal を削除できてしまう。 | `/api/meal-plans/{任意のID}/meals/{id}` で他のミールプランに属する Meal を削除できる。 | `deleteMeal` でサービス層に `mealPlanId` を渡し、削除対象 Meal の `mealPlanId` と一致するか検証する。または `MealService.deleteMeal` のシグネチャを `deleteMeal(Long mealPlanId, Long id)` に変更する。 | 完了 | `MealService.deleteMeal` のシグネチャを `deleteMeal(Long mealPlanId, Long id)` に変更。`MealServiceImpl.deleteMeal` で Meal を取得後に `mealPlanId` の一致を検証し、不一致の場合は `ResourceNotFoundException` をスロー。`MealController.deleteMeal` で `mealPlanId` をサービスに渡すよう修正した。 |
| R206 | **設計** | `controller/MealController.java` | `updateMeal` で `mealPlanId` をサービス層に渡しているが、`MealServiceImpl.updateMeal` では `mealPlanId` を使った所属チェックが行われていない。 | 異なるミールプランに属する Meal を更新できてしまう。 | `updateMeal` でも `getMealById` と同様に、取得した Meal の `mealPlanId` と引数の `mealPlanId` が一致するか検証する。 | 完了 | `MealService.updateMeal` のシグネチャを `updateMeal(Long mealPlanId, Long id, MealDto)` に変更。`MealServiceImpl.updateMeal` で Meal 取得後に `mealPlanId` の一致を検証し、不一致の場合は `ResourceNotFoundException` をスロー。`getMealById` / `deleteMeal` と同一パターンで統一した。 |
| R207 | **設計** | `model/UserGoalDto.java` | `targetWeight`（`Double`）と `activityLevel`（`String`）フィールドが定義されているが、`UserGoal` エンティティにはこれらのフィールドが存在しない。`UserGoalServiceImpl` の `toEntity` / `toDto` でもこれらのフィールドはマッピングされておらず、登録・取得時に値が失われる。 | リクエストで `targetWeight` や `activityLevel` を送信しても保存されず、サイレントに無視される。 | `UserGoal` エンティティに `targetWeight` / `activityLevel` フィールドを追加してマッピングするか、不要であれば `UserGoalDto` から削除する。 | 完了 | `UserGoal` エンティティに `targetWeight` / `activityLevel` フィールドは追加済み、`toEntity` / `toDto` のマッピングも実装済みだった。`updateUserGoal` でこれらのフィールドの更新が漏れていたため追加し、登録・取得・更新すべての操作で値が正しく反映されるよう修正した。 |
| R208 | **設計** | `service/impl/MealServiceImpl.java` | `toDto` メソッドで `calories` と `protein` フィールドがマッピングされていない。`Meal` エンティティにもこれらのフィールドが存在しないため、`MealDto` の `calories` / `protein` は常に `null` になる。 | カロリー・タンパク質情報が API レスポンスに含まれない。 | `Meal` エンティティに `calories` / `protein` フィールドを追加し、`toEntity` / `toDto` でマッピングする。 | **完了** | `Meal` エンティティにはすでに `calories`（`total_calories`）・`protein` フィールドが追加済みだったため、`MealServiceImpl` の `toEntity`・`toDto`・`updateMeal` に `calories` / `protein` のマッピングを追加した。 |
| R209 | **設計** | `entity/UserGoal.java` | `userId` を `Long` 型のカラムとして持ち、`User` エンティティとの JPA リレーション（`@ManyToOne`）が設定されていない。`UserGoalServiceImpl.createUserGoal` でユーザー存在確認は行われているが、DB レベルの外部キー制約が機能しない。 | アプリ外から直接 DB を操作した場合に参照整合性が保証されない。 | `UserGoal` に `@ManyToOne` で `User` との関連を設定し、`@JoinColumn(name = "user_id")` で外部キーを定義することを検討する。 | **完了** | `UserGoal` エンティティの `userId`（Long型）を `@ManyToOne(fetch = FetchType.LAZY, optional = false)` + `@JoinColumn(name = "user_id", nullable = false, unique = true)` による `User` リレーションに変更済み。`UserGoalRepository` を `findByUser(User user)` に変更し、`UserGoalServiceImpl` 全メソッドで `UserRepository` から `User` エンティティを取得してリレーション経由で操作するよう実装済み。DB レベルの外部キー制約が機能する状態になっている。 |
| R210 | **設計** | `model/MealDto.java` | `mealDate` フィールドにバリデーションアノテーションが付与されていない。過去日・未来日を問わず任意の日付が登録できる。 | 要件によっては不正な日付（例：遠い未来や過去）が登録されるリスクがある。 | 要件に応じて `@FutureOrPresent` や `@PastOrPresent` などのバリデーションアノテーションを追加することを検討する。 | 完了 | 本アプリは過去の献立記録と将来の献立計画の両方を扱うため、`@FutureOrPresent` / `@PastOrPresent` による一方向制約は不適切と判断。日付未入力を防ぐ `@NotNull` を追加した。 |
| R211 | **設計** | `service/impl/RecipeServiceImpl.java` | `searchRecipesByTitle` で `keyword` が空文字の場合、`findByTitleContaining("")` が呼ばれ全件検索と同等の結果が返る。`RecipeController` 側で空文字チェックを行い `getAllRecipes()` にフォールバックしているが、サービス層では考慮されていない。直接サービスを呼び出す場合に意図しない全件取得が発生する可能性がある。 | サービス層を直接利用するテストや将来の呼び出し元で意図しない全件取得が発生する。 | `searchRecipesByTitle` 内でも `keyword` が空文字の場合は `findAll()` にフォールバックするか、`IllegalArgumentException` をスローする。 | 完了 | `searchRecipesByTitle` 内で `keyword` が `null` または空文字の場合に `getAllRecipes()` へフォールバックする処理を実装済み。サービス層単独で呼び出した場合も意図しない全件取得が発生しない。 |
| R212 | **品質** | `entity/Meal.java` | `calories` / `protein` フィールドが存在しない（`MealDto` には定義されているが `Meal` エンティティには未定義）。`MealDto` の `calories` / `protein` は常に `null` になる（R208 と関連）。 | 栄養素情報が永続化されない。 | `Meal` エンティティに `calories` / `protein` フィールドを追加する。 | **完了** | R208 対応時に `Meal` エンティティへ `calories`（`@Column(name = "total_calories")`）と `protein`（`@Column(name = "protein")`）フィールドを追加済み。`MealServiceImpl` の `toEntity` / `toDto` / `updateMeal` でもマッピング済み。 |
| R213 | **品質** | `entity/MealPlan.java` | `title` / `goal` フィールドに `@Column` アノテーションが付与されておらず、カラム名・制約が明示されていない。他のエンティティ（`Recipe`・`UserGoal`）では `@Column` が明示されており、一貫性がない。 | カラム名がフィールド名から自動生成されるため意図しない名前になる可能性がある。 | `@Column(name = "title")` / `@Column(name = "goal")` を追加し、一貫性を保つ。 | **完了** | `title` に `@Column(name = "title")`、`goal` に `@Column(name = "goal")` を追加。他のエンティティと同様に `@Column` を明示し、一貫性を確保した。 |
| R214 | **品質** | `service/UserService.java` | インターフェースのメソッドに Javadoc コメントが付与されていない。`UserGoalService` では全メソッドに Javadoc が付与されており、一貫性がない。 | コードの可読性・保守性が低下する。 | 全メソッドに Javadoc コメントを追加する。 | 完了 | `createUser` / `getAllUsers` / `getUserById` / `updateUser` / `deleteUser` の全5メソッドに Javadoc コメントを追加した。 |
| R215 | **品質** | `static/js/app.js` | `renderTable` 内で `recipe.calories` が `null` の場合、`String(null)` が `"null"` という文字列になり、`escapeHtml("null")` を経て画面に `"null kcal"` と表示される。 | カロリー未設定のレシピで `"null kcal"` という不正な表示になる。 | `recipe.calories != null ? recipe.calories : '-'` のように null ガードを追加する。 | 完了 | `recipe.calories != null ? escapeHtml(String(recipe.calories)) : '-'` に修正。`null` の場合は `'-'` を表示し、値がある場合は `escapeHtml` を通して安全に描画するよう対応した。 |
| R216 | **品質** | `static/index.html` | フォームの `<label>` 要素が `for` 属性を持っておらず、対応する `<input>` の `id` と紐付けられていない。 | アクセシビリティ上の問題があり、ラベルクリックでフォーカスが移動しない。 | 各 `<label>` に `for` 属性を追加し、対応する `<input>` の `id` と一致させる。 | **完了** | タイトル・説明・カロリーの3つの `<label>` に `for="title"`・`for="description"`・`for="calories"` を追加し、対応する入力要素と紐付けた。 |

---

## 4. 総評および次のステップ

### 総評

前回レビューからの改善が多数確認でき、コード全体の品質は大幅に向上しています。今回の指摘は主に「ネストされたリソース（Meal）の所属チェック漏れ」「ユーザー登録・更新時の重複チェック欠如」「エンティティと DTO のフィールド不一致」の3点に集中しており、これらは機能的な問題に直結するため優先対応を推奨します。

### 優先度別 次のステップ

**高優先度（機能・セキュリティに直結）**
1. R203 / R204: ユーザー登録・更新時の重複チェックと `DataIntegrityViolationException` ハンドリング
2. R205 / R206: `MealController` の `deleteMeal` / `updateMeal` における `mealPlanId` 所属チェック
3. R208 / R212: `Meal` エンティティへの `calories` / `protein` フィールド追加とマッピング

**中優先度（設計改善）**
4. R201 / R202: `UserDto` / `UserCreateRequest` のバリデーション強化
5. R207: `UserGoalDto` の `targetWeight` / `activityLevel` とエンティティの不一致解消
6. R209: `UserGoal` への `@ManyToOne` リレーション設定
7. R211: `RecipeServiceImpl.searchRecipesByTitle` の空文字対応

**低優先度（品質改善）**
8. R210: `MealDto.mealDate` のバリデーション追加
9. R213: `MealPlan` エンティティの `@Column` 追加
10. R214: `UserService` への Javadoc 追加
11. R215: `app.js` の `calories` null ガード追加
12. R216: `index.html` の `<label for>` 属性追加

---

*このレビュー記録票は指摘事項ごとにステータスを管理します。対応完了後は「ステータス」欄を「完了」に更新し、「対応結果」欄に実施内容を記載してください。*
