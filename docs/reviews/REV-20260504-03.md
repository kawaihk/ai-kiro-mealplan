# レビュー記録票: src/test 配下テストコードレビュー

## 1. レビュー情報

- **レビューID**: REV-20260504-03
- **レビュー対象**: `src/test` 配下の全テストコード
- **レビュアー**: Kiro
- **レビュー日**: 2026-05-04
- **ステータス**: 指摘あり

---

## 2. チェック結果サマリー

コントローラ層（`@WebMvcTest`）とサービス実装層（`@ExtendWith(MockitoExtension.class)`）のテストは正常系・異常系ともに網羅的に実装されており、品質は高い水準にあります。一方で、`service/` 直下の旧インターフェーステスト（`MealServiceTest`・`UserGoalServiceTest`・`UserServiceTest`）が現在のクラス構成と乖離してコンパイルエラーになっており、早急な対応が必要です。また、エンティティ・モデル・リポジトリ・バリデーションアノテーション等のテストクラスが空実装のままのものが多数あります。

---

## 3. 指摘事項

| ID | 分類 | 対象ファイル | 内容 | 影響/リスク | 対応方針 | ステータス | 対応結果 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| T001 | **コンパイルエラー** | `service/MealServiceTest.java` | `com.aimealplan.model.Meal` をインポートしているが存在しない。`MealService` インターフェースに `registerMeal` / `getMealsByUserId` メソッドが存在しない。`MealRepository.findByUserId` も存在しない。現在の実装と完全に乖離している。 | ビルドエラーが発生しテストが実行できない。 | 現在の `MealServiceImpl` / `MealService` インターフェースに合わせてテストを書き直すか、`MealServiceImplTest` に統合して削除する。 | **完了** | `MealServiceTest` を `MealService` インターフェースのコントラクトテストとして書き直した。`@Mock MealService` を使用し、`createMeal` / `getMealsByMealPlanId` / `getMealById` / `updateMeal` / `deleteMeal` の5メソッドが現在のシグネチャで呼び出せることを検証。詳細なロジック検証は `MealServiceImplTest` に委譲する設計とした。 |
| T002 | **コンパイルエラー** | `service/UserGoalServiceTest.java` | `com.aimealplan.model.UserGoal` をインポートしているが存在しない（`entity.UserGoal` が正しい）。`UserGoalService` インターフェースに `saveGoal` / `getGoalByUserId` メソッドが存在しない。`UserGoalRepository.findByUserId` も存在しない（`findByUser(User)` が正しい）。 | ビルドエラーが発生しテストが実行できない。 | 現在の `UserGoalServiceImpl` / `UserGoalService` インターフェースに合わせてテストを書き直すか、`UserGoalServiceImplTest` に統合して削除する。 | 完了 | `UserGoalServiceImplTest` に `createUserGoal` / `getUserGoalByUserId` / `updateUserGoal` / `deleteUserGoal` の正常系・異常系テストが現在の実装に合わせて実装済みであることを確認。`UserGoalServiceTest.java` を削除して統合した。 |
| T003 | **コンパイルエラー** | `service/UserServiceTest.java` | `com.aimealplan.model.User` をインポートしているが存在しない（`entity.User` が正しい）。`UserService` インターフェースに `registerUser` メソッドが存在しない（`createUser` が正しい）。`UserService.getUserById` の戻り値型が `Optional<User>` ではなく `Optional<UserDto>` である。 | ビルドエラーが発生しテストが実行できない。 | 現在の `UserServiceImpl` / `UserService` インターフェースに合わせてテストを書き直すか、`UserServiceImplTest` に統合して削除する。 | 完了 | 現在の `UserService` インターフェース・`UserServiceImpl` に合わせてテストを全面書き直し。`com.aimealplan.entity.User` / `UserDto` を正しくインポートし、`registerUser` を `createUser(UserDto, String)` に、`getUserById` の戻り値を `Optional<UserDto>` に修正。`UserServiceImplTest` との重複を避け、インターフェースのコントラクト検証テストとして整理した。 |
| T004 | **コンパイルエラー** | `service/impl/UserGoalServiceImplTest.java` | `getUserGoalByUserId` のテストで `userGoalRepository.findByUserId(1L)` を呼び出しているが、現在の `UserGoalRepository` には `findByUserId` メソッドが存在しない（`findByUser(User)` が正しい）。また、`UserGoalServiceImpl.getUserGoalByUserId` は `userRepository.findById` でユーザー存在確認を行ってから `findByUser` を呼ぶ実装になっているが、テストではその `userRepository.findById` のスタブが設定されていない。 | テストが正しく動作しない。 | `getUserGoalByUserId` のテストを現在の実装（`userRepository.findById` → `userGoalRepository.findByUser`）に合わせて修正する。 | **完了** | `UserGoalRepository` には `findByUserId(Long userId)` が定義済みであり、`UserGoalServiceImpl.getUserGoalByUserId` も `findByUserId` を直接呼ぶ実装（ユーザー存在確認なし）になっていることを確認。テストのスタブ（`findByUserId`）は実装と一致しており、コンパイルエラーも発生していないため対応不要と判断した。 |
| T005 | **未実装** | `entity/MealPlanTest.java` / `entity/MealTest.java` / `entity/RecipeTest.java` / `entity/UserTest.java` | 4ファイルすべてが空クラス（テストメソッドなし）。 | エンティティの Lombok アノテーション（`@Builder`・`@EqualsAndHashCode`・`@ToString` 等）の動作が検証されていない。 | 各エンティティの `@Builder` によるオブジェクト生成・`@EqualsAndHashCode` の動作・`@ToString` の循環参照除外などの基本的なテストを実装する。 | **完了** | 4ファイルすべてにテストを実装。各エンティティで `@Builder`（全フィールド生成）・`@NoArgsConstructor`・`@Getter/@Setter`・`@EqualsAndHashCode`（除外フィールドの動作確認）・`@ToString`（循環参照除外の確認）を検証。`Meal` は `MealType` enum の全値確認、`User` は `Role` enum の確認と `toString` へのパスワード含有の注意点コメントも追加。 |
| T006 | **未実装** | `exception/ErrorResponseTest.java` / `exception/ResourceNotFoundExceptionTest.java` | 2ファイルが空クラス。 | `ErrorResponse` の Builder パターンや `ResourceNotFoundException` のメッセージ伝播が検証されていない。 | `ErrorResponse.builder()` による生成・各フィールドの設定、`ResourceNotFoundException` のメッセージ・継承関係のテストを実装する。 | 完了 | `ErrorResponseTest` に Builder による全フィールド設定・`details` 単数/複数・`ErrorDetail.Builder` の4テストを実装。`ResourceNotFoundExceptionTest` にメッセージ伝播・`RuntimeException` 継承確認・スロー時の catch 検証の3テストを実装した。 |
| T007 | **未実装** | `model/MealDtoTest.java` / `model/UserDtoTest.java` / `model/UserGoalDtoTest.java` | 3ファイルが空クラス。 | DTO のバリデーションアノテーション（`@NotNull`・`@PositiveOrZero`・`@Min`・`@Max`・`@PfcRatioSum` 等）の動作が検証されていない。 | Jakarta Validation の `Validator` を使用して、各 DTO のバリデーション正常系・異常系テストを実装する。 | 完了 | `Validation.buildDefaultValidatorFactory()` で `Validator` を生成し、各 DTO のバリデーションを検証するテストを実装。`MealDtoTest`: `@NotNull(mealDate)`・`@PositiveOrZero(calories/protein)` の正常系・異常系。`UserDtoTest`: バリデーションアノテーションなしのため Builder・equals・setter の動作を検証。`UserGoalDtoTest`: `@NotNull(userId)`・`@Min/@Max(PFC比率)`・`@PfcRatioSum`（全null許容・合計100チェック）の正常系・異常系を網羅。 |
| T008 | **未実装** | `repository/MealRepositoryTest.java` / `repository/UserGoalRepositoryTest.java` / `repository/UserRepositoryTest.java` | `MealRepositoryTest` はクラス宣言すらなく空ファイル。他2ファイルも空クラス。 | `findByMealPlanId`・`findByUser`・`findByUsername`・`findByEmail` などのカスタムクエリメソッドが検証されていない。 | `@DataJpaTest` を使用してインメモリ DB（H2 等）でリポジトリのカスタムクエリメソッドをテストする。または `@SpringBootTest` + テスト用 DB で統合テストを実装する。 | **完了** | `pom.xml` に H2 テスト依存を追加。`@DataJpaTest` を使用した3ファイルを実装。`UserRepositoryTest`：`findByUsername`・`findByEmail` の正常系・異常系、UNIQUE 制約違反テスト。`UserGoalRepositoryTest`：`findByUser`・`findByUserId` の正常系・異常系、1ユーザー1目標の UNIQUE 制約テスト。`MealRepositoryTest`：`findByMealPlanId` の正常系（複数件・0件）・異常系（存在しない ID）、別 MealPlan の Meal が混入しないことを検証。 |
| T009 | **未実装** | `service/RecipeServiceTest.java` | 空クラス。`RecipeServiceImplTest` は充実しているが、インターフェース経由のテストが存在しない。 | `RecipeService` インターフェースのコントラクトが検証されていない。 | `RecipeServiceImplTest` が十分に実装されているため、このファイルは削除するか、インターフェースのコントラクトテストとして活用する。 | 完了 | `RecipeServiceImplTest` に `createRecipe` / `getAllRecipes` / `getRecipeById` / `updateRecipe` / `deleteRecipe` / `searchRecipesByTitle`（空文字・null フォールバック含む）の正常系・異常系テストが実装済みであることを確認。空クラスの `RecipeServiceTest.java` を削除して統合した。 |
| T010 | **未実装** | `validation/PfcRatioSumTest.java` | 空クラス。`PfcRatioSumValidatorTest` は充実しているが、`@PfcRatioSum` アノテーション自体のメタアノテーション（`@Target`・`@Retention`・`@Constraint` 等）の検証がない。 | アノテーション定義の正確性が検証されていない。 | `@PfcRatioSum` アノテーションの `@Target`・`@Retention`・`@Constraint` の設定が正しいことを確認するテストを実装する。または空クラスを削除する。 | **完了** | `PfcRatioSumTest` にアノテーション定義の検証テストを実装。`@Target`（TYPE のみ）・`@Retention`（RUNTIME）・`@Constraint`（`PfcRatioSumValidator` で検証）・`@Documented` の存在確認、および `message`・`groups`・`payload` のデフォルト値を Java リフレクションで検証する7テストを追加した。 |
| T011 | **設計** | `service/impl/UserGoalServiceImplTest.java` | `getUserGoalByUserId` の正常系テストで `userGoalRepository.findByUserId(1L)` を呼び出しているが、実装では `userGoalRepository.findByUser(user)` を使用している。テストが実装と乖離しており、テストが通っていたとしても実装の正しさを検証できていない。 | テストが実装の実際の動作を検証していない（偽陽性のリスク）。 | `getUserGoalByUserId` のテストを `userRepository.findById(1L)` → `userGoalRepository.findByUser(user)` の順でスタブを設定するよう修正する。 | 完了 | R302 対応で `getUserGoalByUserId` の実装が `userGoalRepository.findByUserId(userId)` を直接呼ぶ形に変更済み。`UserGoalServiceImplTest` の `getUserGoalByUserId_success` / `getUserGoalByUserId_notFound` は `findByUserId` をスタブする実装になっており、現在の実装と一致していることを確認した。 |
| T012 | **設計** | `exception/GlobalExceptionHandlerTest.java` | `handleResourceNotFoundException` テストで `recipeService.getRecipeById(99L)` が `Optional.empty()` を返すようスタブしているが、`ResourceNotFoundException` をスローしていない。コントローラ側で `orElseThrow` により `ResourceNotFoundException` がスローされる動作を検証しているが、`GlobalExceptionHandler` が `ResourceNotFoundException` を直接ハンドリングする動作の検証になっていない。 | `GlobalExceptionHandler` の `handleResourceNotFoundException` が実際に呼ばれているかどうかが不明確。 | `recipeService.getRecipeById(99L)` が `ResourceNotFoundException` を直接スローするようスタブを変更し、ハンドラーが確実に呼ばれることを検証する。 | 完了 | `recipeService.getRecipeById(99L)` のスタブを `thenReturn(Optional.empty())` から `thenThrow(new ResourceNotFoundException("Recipe not found with id: 99"))` に変更。`GlobalExceptionHandler.handleResourceNotFoundException` が直接呼ばれることを検証し、レスポンスの `message` フィールドも合わせて検証するよう強化。不要になった `import java.util.Optional` も削除した。 |
| T013 | **設計** | `controller/UserControllerTest.java` | `createUser_success` テストで `userService.createUser(any(UserDto.class), eq("password123"))` とスタブしているが、コントローラ側で `request.getPassword()` を渡しているため、`eq("password123")` の引数マッチャーが正しく機能するかはリクエストの内容に依存する。パスワードが正しくサービス層に渡されているかの検証が不十分。 | パスワードの受け渡しが正しく検証されていない。 | `verify(userService).createUser(any(UserDto.class), eq("password123"))` を追加してパスワードの受け渡しを明示的に検証する。 | **完了** | `createUser_success` テストの末尾に `verify(userService).createUser(any(UserDto.class), eq("password123"))` を追加。リクエストボディの `password` フィールドがサービス層に正しく渡されていることを明示的に検証するようにした。 |
| T014 | **品質** | `service/impl/UserGoalServiceImplTest.java` | `createUserGoal_success` テストで `assertThat(result.getProteinRatio()).isEqualTo(30)` を検証しているが、`UserGoal` エンティティの `targetProtein` が `Integer` 型で `UserGoalDto` の `proteinRatio` も `Integer` 型であるため、変換精度の問題（旧 R107 指摘）が解消されているかの確認テストとして機能している。ただし `targetWeight` / `activityLevel` のマッピング検証が含まれていない。 | `targetWeight` / `activityLevel` のマッピングが正しく動作しているかが検証されていない。 | `createUserGoal_success` テストに `targetWeight` / `activityLevel` の検証を追加する。 | **完了** | `@BeforeEach` の `userGoal`・`userGoalDto` に `targetWeight(65.5)`・`activityLevel("MODERATE")` を追加。`createUserGoal_success` に `assertThat(result.getTargetWeight()).isEqualTo(65.5)` と `assertThat(result.getActivityLevel()).isEqualTo("MODERATE")` の検証を追加し、全フィールドのマッピングを網羅した。 |
| T015 | **品質** | `controller/MealControllerTest.java` | `createMeal_success` テストで `mealService.createMeal(any(MealDto.class))` とスタブしているが、コントローラが `mealDto.setMealPlanId(mealPlanId)` を呼び出してから `createMeal` に渡すことの検証がない。`mealPlanId` が正しく DTO にセットされているかが未検証。 | `mealPlanId` のセット漏れがあってもテストが通ってしまう。 | `verify(mealService).createMeal(argThat(dto -> dto.getMealPlanId().equals(1L)))` を追加して `mealPlanId` のセットを検証する。 | 完了 | `createMeal_success` テストに `verify(mealService).createMeal(argThat(dto -> dto.getMealPlanId().equals(1L)))` が実装済みであることを確認した。コントローラが `mealDto.setMealPlanId(mealPlanId)` を呼び出してサービスに渡すことが正しく検証されている。 |

---

## 4. 総評および次のステップ

### 総評

`MealServiceImplTest`・`RecipeServiceImplTest`・`UserServiceImplTest`・`UserGoalServiceImplTest` の4つのサービス実装テストは正常系・異常系ともに充実しており、品質は高い水準にあります。コントローラテストも `@WebMvcTest` を適切に使用し、バリデーション・例外ハンドリングまで網羅的に検証されています。一方で、旧インターフェーステスト（T001〜T003）のコンパイルエラーが最優先課題です。

### 優先度別 次のステップ

**高優先度（ビルドエラー解消）**
1. T001: `MealServiceTest` を現在の実装に合わせて修正または削除
2. T002: `UserGoalServiceTest` を現在の実装に合わせて修正または削除
3. T003: `UserServiceTest` を現在の実装に合わせて修正または削除
4. T004: `UserGoalServiceImplTest.getUserGoalByUserId` のスタブを修正

**中優先度（テスト品質向上）**
5. T011: `UserGoalServiceImplTest` の `getUserGoalByUserId` テストを実装に合わせて修正
6. T012: `GlobalExceptionHandlerTest.handleResourceNotFoundException` のスタブを修正
7. T013: `UserControllerTest.createUser_success` にパスワード受け渡し検証を追加
8. T015: `MealControllerTest.createMeal_success` に `mealPlanId` セット検証を追加

**低優先度（テスト追加）**
9. T005: エンティティテストの実装
10. T006: 例外クラステストの実装
11. T007: DTO バリデーションテストの実装
12. T008: リポジトリテストの実装
13. T009: `RecipeServiceTest` の整理（削除または活用）
14. T010: `PfcRatioSumTest` の整理（削除または実装）
15. T014: `UserGoalServiceImplTest` に `targetWeight` / `activityLevel` 検証を追加

---

*このレビュー記録票は指摘事項ごとにステータスを管理します。対応完了後は「ステータス」欄を「完了」に更新し、「対応結果」欄に実施内容を記載してください。*
